# Keep

One sentence: is this the right small product to build next? If the
slice is a spec, say whether it matches `PRD.md` § 4.

# Can't check

Quote every acceptance criterion or done-criterion box the student
cannot verify by clicking or typing. If none, write `none`.

# Missing surprise

Which of these is missing: empty/first visit, blank or bad input,
refresh (the item they just added is still there)? If the slice does
not persist, take input, or show a list, say `n/a` and why. If none
missing, write `none`.

# Assumptions

Up to 3 things the writer decided that the student never said. If
none, write `none`.

# Fix these 3

The only changes worth making before the next build. Each one
sentence. If fewer than three matter, write fewer. The student
decides — you are not the boss.
