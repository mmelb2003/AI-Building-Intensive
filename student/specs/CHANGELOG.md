# Spec pack changelog

History of the **workshop spec materials** — the template, index seed,
Loop example, and the spec-writing skill in `cursor-pack/skills/`.

This is not the change log inside an individual spec. Those stay in
each spec's § 12.

Newest first. Add a dated entry whenever you change a file in this
folder or `cursor-pack/skills/spec-writing/`. Name the files and why.

---

## 2026-08-22

- **Added** `student/specs/` — `_template.md` (workshop-tuned blank spec),
  `README-seed.md` (copy into an app as `specs/README.md`),
  `Spec-Example-Loop.md` (filled spec 001 for Loop submit + list),
  and this changelog.
- **Added** `student/cursor-pack/skills/spec-writing/` — the spec-writing
  skill students copy into `.cursor/skills/spec-writing/`.
- **Updated** `student/README.md`, `student/prd/README.md`, and
  `student/cursor-pack/README.md` so specs start after the first screen
  is on, not during the day-one setup.
