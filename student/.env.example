# Copy this file into your app project as `.env`, then paste your OpenAI key.
# Never commit `.env`. Never put keys in client code.
#
# The model is hardcoded in code as gpt-4o-mini — do not add a model env var.
# DATABASE_URL / POSTGRES_URL are added later when you connect Neon (Vercel may set them).

OPENAI_API_KEY=
